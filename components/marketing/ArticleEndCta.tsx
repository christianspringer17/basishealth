import { ConversionBand } from "@/components/marketing/ConversionBand";

/** Single conversion band at the end of Learn articles — pricing + waitlist */
export function ArticleEndCta() {
  return (
    <ConversionBand
      title="Ready for a metabolism-first plan?"
      description="Explore GLP–One pricing or join the waitlist for launch updates."
    />
  );
}
